[x] P0-1: Remove hardcoded assignee enum from schema (commit 0beb9e2)
  - Changed assignee from enum('eduardo','hawkbot') to plain text() in schema.ts
  - Removed hardcoded DEFAULT from migration in db/index.ts
  - TaskCreateModal: fetches team from /api/team, builds assignee dropdown dynamically
  - TaskCard: accepts teamMembers prop, resolves assignee emoji+name from team data
  - tasks.vue: fetches team members via useQuery, passes to TaskCard
  - All API endpoints (GET/POST/PATCH/DELETE): resolve actor from team member lookup
  - Dispatcher: looks up assignee by team_members.id instead of name
  - Lint + typecheck pass
[x] P0-2: Replace hardcoded DEFAULT_TEAM seed with config-driven import
  - server/utils/seed.ts: rewrote to read ~/.openclaw/openclaw.json agents.list
  - Infers emoji and role from agent name (dev→💻, research→🔍, ops→⚙️, writer→✍️)
  - Adds current OS user as human "owner" member
  - Falls back to minimal team (1 human owner + 1 assistant agent) if config not found
  - Only seeds if team table is completely empty — never overwrites existing records
  - server/db/schema.ts: added openclaw_agent_id and agent_dir columns to team_members
  - server/db/index.ts: added ALTER TABLE migrations for new columns + CREATE TABLE update
  - Lint + typecheck pass
[x] P0-3: Add Team CRUD API endpoints
  - server/api/team/index.post.ts: create team member with validation (name required, memberType must be human|agent, openclawAgentId unique)
  - server/api/team/[id].patch.ts: update team member fields (partial update, validates memberType + openclawAgentId uniqueness)
  - server/api/team/[id].delete.ts: hard-delete team member with 404 check
  - All endpoints: log to activityLog + broadcast via SSE (team_updated event)
  - Follows same patterns as task CRUD endpoints (uuid, timestamps, broadcastToClients)
  - Lint + typecheck pass
[x] P0-4: Add Team management UI
  - app/components/TeamMemberModal.vue: create/edit modal with name, type, emoji, role, model, description, specialties, openclawAgentId fields
  - app/pages/team.vue: upgraded from read-only display to full CRUD
  - "Add Member" button in header opens create modal
  - Hover dropdown menu on each card with Edit and Delete actions
  - Edit action opens modal pre-populated with member data
  - Delete action shows confirmation modal, then DELETEs via API
  - Switched from useFetch to TanStack useQuery with 10s polling + invalidation on mutations
  - Lint + typecheck pass
[x] P1-1: Create Settings page
  - server/db/schema.ts: added `settings` table (key TEXT PK, value TEXT, updated_at TEXT)
  - server/db/index.ts: added CREATE TABLE IF NOT EXISTS settings migration
  - server/api/settings/index.get.ts: returns all settings as key-value object
  - server/api/settings/index.patch.ts: upsert settings with validation (allowed keys: gateway_url, gateway_token, workspace_path, main_session_id)
  - app/pages/settings.vue: settings form with Gateway URL, Gateway Token (masked), Workspace Path, Main Session ID
  - app/layouts/default.vue: added Settings nav item to sidebar
  - Form loads from DB, tracks changes, saves via PATCH, shows success/error feedback
  - Lint + typecheck pass
[x] P1-2: Make MAIN_SESSION_ID configurable
  - server/utils/dispatcher.ts: removed hardcoded UUID constant
  - Added getMainSessionId() helper that reads `main_session_id` from settings table
  - dispatchTask() now checks for session ID before dispatching; logs warning and skips if not set
  - spawnAgent() accepts sessionId as parameter instead of using global constant
  - Lint + typecheck pass
[x] P1-3: Fix hardcoded Portuguese in dispatch prompt
  - server/utils/dispatcher.ts: translated buildPrompt from Portuguese to English
  - Extracted DEFAULT_DISPATCH_PROMPT constant with template variables ({{agent_emoji}}, {{agent_name}}, {{agent_specialties}}, {{task_title}}, {{task_description}}, {{task_id}})
  - Added getDispatchPromptTemplate() helper that reads `dispatch_prompt_template` from settings table, falls back to default
  - buildPrompt() now uses string replacement on the template instead of hardcoded interpolation
  - server/api/settings/index.patch.ts: added `dispatch_prompt_template` to ALLOWED_KEYS
  - app/pages/settings.vue: added Dispatch Prompt Template textarea field with variable reference docs
  - Lint + typecheck pass
